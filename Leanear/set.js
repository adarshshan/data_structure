const set=new Set([3,4,5,6]);
set.add(33)//to add an element
// set.delete(3)//to delete perticular element
console.log(set.has(5))
set.delete(3);
set.
console.log(`The size of the code is ${set.size}`)
for (let item of set){
    console.log(item)
}
